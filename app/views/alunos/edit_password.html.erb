<% provide(:title, 'Redefinir senha de Aluno') %>
<% provide(:button_text, 'Salvar Alterações') %>
<% provide(:path, aluno_update_password_path(@aluno)) %>

<h1>Redefinir senha de Aluno</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form_update_aluno_password' %>
  </div>
</div>