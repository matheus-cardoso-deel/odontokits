<% provide(:title, @kit_type.type_name) %>
<div class="row">
  <aside class="col-md-4">

    <section class="user_info">
      <h1>
          <div style="margin-bottom: 10px">
            <%= @kit_type.type_name %>
          </div>
          <div>
            <% if current_user.admin? %>
               <%= link_to "Editar Tipo de Caixa", edit_kit_type_path(@kit_type), class: 'btn btn-primary', style: 'float: none'%>
               <%= link_to "Excluir Tipo de Caixa", @kit_type, #analogous kit_path(@kit)
               method: :delete, data: { confirm: "Você tem certeza que deseja excluir #{@kit_type.type_name}?" }, class: 'btn btn-danger', style: 'float: none'%>
            <% end %>
            </div>
      </h1>
    </section>
  </aside>
</div>